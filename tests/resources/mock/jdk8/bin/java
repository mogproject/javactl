#!/bin/sh

if [ "$*" = "-version" ]; then
    {
        echo 'java version "1.8.0_31"'
        echo 'Java(TM) SE Runtime Environment (build 1.8.0_31-b13)'
        echo 'Java HotSpot(TM) 64-Bit Server VM (build 25.31-b07, mixed mode)'
    } >&2
    exit 0
fi

echo "$@"

